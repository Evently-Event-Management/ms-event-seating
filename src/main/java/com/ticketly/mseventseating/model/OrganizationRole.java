package com.ticketly.mseventseating.model;

/**
 * Defines the roles a user can have within a specific organization.
 */
public enum OrganizationRole {
    /**
     * A role specifically for staff who only need to scan tickets at events.
     */
    SCANNER
}
