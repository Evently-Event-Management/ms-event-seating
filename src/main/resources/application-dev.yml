spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/event_seating
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

# AWS S3 Configuration for LocalStack
aws:
  s3:
    endpoint: http://localhost:4566
    region: us-east-1
    bucket-name: event-seating-uploads
    access-key: test
    secret-key: test
