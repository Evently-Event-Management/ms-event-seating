spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/event_seating
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
#      password: ${REDIS_PASSWORD:} # Leave empty if no password
      timeout: 2000
      # Uncomment if using Redis SSL
      # ssl: true

aws:
  s3:
    region: us-east-1
    endpoint: http://localhost:4566
    bucket-name: event-seating-uploads
    access-key: test
    secret-key: test
